const steps=[...document.querySelectorAll('.step')];const nextButtons=[...document.querySelectorAll('.next-button')];const dots=[...document.querySelectorAll('.dot')];const restartButton=document.querySelector('.restart-button');const currentNumber=document.querySelector('#current-number');const heartField=document.querySelector('#heart-field');const picker=document.querySelector('.theme-picker');const toggle=document.querySelector('.theme-toggle');const options=[...document.querySelectorAll('.theme-option')];let currentStep=0,locked=false;function showStep(index){if(locked||index<0||index>=steps.length||index===currentStep)return;locked=true;steps.forEach((step,i)=>{const active=i===index;step.classList.toggle('is-active',active);step.setAttribute('aria-hidden',String(!active));if(active)step.scrollTop=0});dots.forEach((dot,i)=>dot.classList.toggle('is-active',i===index));currentStep=index;if(currentNumber)currentNumber.textContent=String(index+1).padStart(2,'0');setTimeout(()=>locked=false,720)}nextButtons.forEach(b=>b.addEventListener('click',()=>showStep(currentStep+1)));dots.forEach(d=>d.addEventListener('click',()=>showStep(Number(d.dataset.go))));restartButton?.addEventListener('click',()=>showStep(0));document.addEventListener('keydown',e=>{if(document.activeElement?.tagName==='IFRAME')return;if(e.key==='ArrowRight'||e.key==='Enter')showStep(Math.min(currentStep+1,steps.length-1));if(e.key==='ArrowLeft')showStep(Math.max(currentStep-1,0))});let sx=0,sy=0;document.addEventListener('touchstart',e=>{if(e.target.closest('iframe,video'))return;sx=e.changedTouches[0].clientX;sy=e.changedTouches[0].clientY},{passive:true});document.addEventListener('touchend',e=>{if(e.target.closest('iframe,video'))return;const dx=e.changedTouches[0].clientX-sx,dy=e.changedTouches[0].clientY-sy;if(Math.abs(dx)<55||Math.abs(dx)<Math.abs(dy))return;showStep(dx<0?Math.min(currentStep+1,steps.length-1):Math.max(currentStep-1,0))},{passive:true});window.addEventListener('mousemove',e=>{document.documentElement.style.setProperty('--mx',`${e.clientX/window.innerWidth*100}%`);document.documentElement.style.setProperty('--my',`${e.clientY/window.innerHeight*100}%`)});if(heartField){const sizes=[10,13,16,20,24,30,38];for(let i=0;i<36;i++){const heart=document.createElement('span');heart.className='floating-heart';heart.textContent=i%5===0?'♡':'♥';heart.style.left=`${Math.random()*100}%`;heart.style.bottom=`${-12-Math.random()*90}vh`;heart.style.fontSize=`${sizes[Math.floor(Math.random()*sizes.length)]}px`;heart.style.animationDuration=`${13+Math.random()*20}s`;heart.style.animationDelay=`${-Math.random()*30}s`;heart.style.setProperty('--drift',`${-110+Math.random()*220}px`);heart.style.opacity=String(.14+Math.random()*.46);heartField.appendChild(heart)}}toggle?.addEventListener('click',()=>{const open=picker.classList.toggle('is-open');toggle.setAttribute('aria-expanded',String(open))});document.addEventListener('click',e=>{if(!picker?.contains(e.target)){picker?.classList.remove('is-open');toggle?.setAttribute('aria-expanded','false')}});function setTheme(theme){document.body.dataset.theme=theme;options.forEach(o=>o.classList.toggle('is-active',o.dataset.themeChoice===theme));localStorage.setItem('anniversary-theme-v3',theme);picker?.classList.remove('is-open');toggle?.setAttribute('aria-expanded','false')}options.forEach(o=>o.addEventListener('click',()=>setTheme(o.dataset.themeChoice)));const saved=localStorage.getItem('anniversary-theme-v3');if(saved&&options.some(o=>o.dataset.themeChoice===saved))setTheme(saved);[...document.querySelectorAll('.magnetic')].forEach(button=>{button.addEventListener('mousemove',e=>{if(window.innerWidth<900)return;const r=button.getBoundingClientRect();button.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.07}px,${(e.clientY-r.top-r.height/2)*.07}px)`});button.addEventListener('mouseleave',()=>button.style.transform='')});